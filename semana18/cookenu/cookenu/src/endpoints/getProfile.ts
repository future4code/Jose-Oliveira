import { Request, Response } from "express";
import {getTokenData} from "../services/authenticator"


export async function getProfile(req:Request, res:Response){
    try {

      // getTokenData(req.headers.auth)
       
    } catch (error) {
        
    }

}